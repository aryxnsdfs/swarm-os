# Swarm-OS — runtime deps for the Hugging Face Space.
#
# Note: llama-cpp-python[server] is installed *separately* from the Dockerfile
# with CMAKE_ARGS="-DGGML_CUDA=on" so it builds a CUDA-enabled wheel against
# the Space GPU. Do NOT add it here without that flag, or pip will install the
# pre-built CPU-only wheel.

# --- HF integration (new for Space migration) ---
huggingface_hub>=0.24,<1

# --- API + server ---
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0,<1
websockets>=12.0
python-multipart>=0.0.9

# --- Models / clients ---
openai>=1.40.0
requests>=2.32.0
pydantic>=2.7.0,<3

# --- OpenEnv contract ---
openenv-core==0.2.3

# --- Backend dashboard helpers ---
pyyaml>=6.0.2
jsonlines>=4.0.0

# --- Sandbox (kept for import compatibility; CLOUD BYPASS in docker_sandbox.py
#     means no real Docker daemon is required at runtime). ---
docker==7.1.0

# --- Optional Gradio demo (root app.py still references it) ---
gradio>=5,<6
